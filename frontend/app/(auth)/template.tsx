'use client';

import { ReactNode } from 'react';

export default function AuthTemplate({ children }: { children: React.ReactNode }) {
  // This template creates a layout boundary
  return children;
} 